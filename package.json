{
  "name": "near-runner",
  "version": "0.2.0",
  "description": "Write tests in TypeScript/JavaScript to run in a controlled NEAR Sandbox local environment.",
  "repository": "https://github.com/near/runner",
  "author": "Near Inc (team@near.org)",
  "license": "(MIT AND Apache-2.0)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "bn.js": "^5.2.0",
    "fs-extra": "^10.0.0",
    "near-api-js": "near/near-api-js#spike/sandbox-runner-tweaks",
    "near-sandbox": "^0.0.5",
    "promisify-child-process": "^4.1.1",
    "rimraf": "^3.0.2",
    "temp-dir": "^2.0.0"
  },
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "yarn build",
    "test:testnet": "NEAR_RUNNER_NETWORK=testnet jest",
    "test:sandbox": "NEAR_RUNNER_NETWORK=sandbox jest",
    "test": "yarn test:sandbox && yarn test:testnet"
  },
  "files": [
    "dist/"
  ],
  "devDependencies": {
    "@types/bn.js": "^5.1.0",
    "@types/fs-extra": "^9.0.12",
    "@types/jest": "^26.0.24",
    "@types/node": "^16.4.10",
    "@types/rimraf": "^3.0.1",
    "jest": "^27.0.6",
    "ts-jest": "^27.0.4",
    "ts-node": "^10.1.0",
    "typescript": "^4.3.5"
  },
  "engines": {
    "node": ">= 14.0.0",
    "npm": ">= 6.0.0"
  }
}
