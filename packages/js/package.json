{
  "name": "near-runner",
  "version": "0.6.5-alpha.1",
  "description": "Write tests in TypeScript/JavaScript to run in a controlled NEAR Sandbox local environment.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/near/runner-js.git",
    "directory": "packages/runner"
  },
  "author": "Near Inc (team@near.org)",
  "license": "(MIT AND Apache-2.0)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "base64url": "^3.0.1",
    "bn.js": "^5.2.0",
    "borsh": "^0.5.0",
    "bs58": "^4.0.1",
    "callsites": "^4.0.0",
    "fs-extra": "^10.0.0",
    "js-sha256": "^0.9.0",
    "near-api-js": "^0.43.1",
    "near-sandbox": "^0.0.8",
    "near-units": "^0.1.9",
    "node-port-check": "^2.0.1",
    "promisify-child-process": "^4.1.1",
    "pure-uuid": "^1.6.2",
    "rimraf": "^3.0.2",
    "temp-dir": "^2.0.0"
  },
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "yarn build",
    "postinstall": "node scripts/install.js",
    "test:testnet": "NEAR_RUNNER_NETWORK=testnet ava --verbose",
    "test:testnet:ci": "NEAR_RUNNER_DEBUG=true NEAR_RUNNER_NETWORK=testnet ava --verbose",
    "test:sandbox": "yarn build && ava --verbose",
    "test:sandbox:ci": "yarn build && NEAR_RUNNER_DEBUG=true ava --verbose",
    "test": "ava --verbose && yarn test:testnet",
    "clean:accounts": "ts-node ./scripts/delete-accounts.ts"
  },
  "files": [
    "dist/",
    "tsconfig.json",
    "core_contracts",
    "scripts/*.js"
  ],
  "engines": {
    "node": ">= 14.0.0",
    "npm": ">= 6.0.0"
  },
  "devDependencies": {
    "@types/bs58": "^4.0.1"
  }
}
